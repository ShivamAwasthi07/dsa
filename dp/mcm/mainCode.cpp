#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int solve(vector<int> solve){
    
    int n = solve.size();
    vector<vector<int>> dp(n, vector<int> (n, 0));

}

int main(){
    vector<int> matrix;
    
    matrix.push_back(5);
    matrix.push_back(4);
    matrix.push_back(6);
    matrix.push_back(2);
    matrix.push_back(7);

    int a = solve(matrix);

    return 0;
}